# ============================
# Work-Flow Platform — Environment Variables
# Copy to .env and fill in values: cp .env.example .env
# ============================

# ---- Backend (FastAPI) ----
# Database connection (SQLite for dev, PostgreSQL for prod)
DATABASE_URL=sqlite+aiosqlite:////data/workflow.db
# DATABASE_URL=postgresql+asyncpg://user:pass@postgres:5432/workflow

# Temporal server address
TEMPORAL_ADDRESS=temporal:7233

# Temporal task queue name
TEMPORAL_TASK_QUEUE=business-workflow-task-queue

# FastAPI server binding
API_HOST=0.0.0.0
API_PORT=8000

# Internal URL for Worker→FastAPI SSE event push
API_BASE_URL=http://backend:8000

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Log directory (default: relative ./logs)
LOG_DIR=/data/logs

# SQLAlchemy echo (debug SQL queries)
DB_ECHO=false

# ---- Frontend (Next.js) ----
# Backend API URL (injected at build time)
NEXT_PUBLIC_API_URL=http://localhost:8000

# ---- CCCC Integration (Phase 2) ----
# CCCC daemon TCP endpoint (e.g. tcp://cccc-daemon:9765)
# CCCC_DAEMON_ADDR=tcp://cccc:9765
# CCCC data directory
# CCCC_HOME=/cccc-data
# CCCC group ID for workflow source
# CCCC_GROUP_ID=g_workflow_api

# ---- Jira Integration (Optional) ----
# JIRA_URL=https://company.atlassian.net
# JIRA_EMAIL=user@company.com
# JIRA_API_TOKEN=your-api-token

# ---- API Keys ----
# ANTHROPIC_API_KEY=sk-ant-...
